services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - PORT=${PORT:-3000}
      - ARIA_DATA_DIR=/app/data
    restart: unless-stopped
    volumes:
      - aria-data:/app/data

volumes:
  aria-data:
